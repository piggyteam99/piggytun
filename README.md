# 🐷 PiggyTun - HAProxy Multi-Port Tunnel

یک اسکریپت هوشمند و خودکار برای ایجاد تونل‌های **Multi-Port** با استفاده از **HAProxy** جهت افزایش سرعت و پایداری اتصال بین سرور ایران و خارج.

---

### 🚀 نصب و اجرا (Installation)

برای اجرای اسکریپت، دستور زیر را کپی کرده و در ترمینال سرور (ایران یا خارج) وارد کنید:

```bash
bash <(curl -Ls https://raw.githubusercontent.com/piggyteam99/piggytun/main/piggytun.sh)

```

**روش جایگزین (در صورت عدم نصب بودن curl):**

```bash
bash <(wget -qO- https://raw.githubusercontent.com/piggyteam99/piggytun/main/piggytun.sh)

```

---

### 📝 معرفی پروژه (Description)

**PiggyTun** با بهره‌گیری از تکنیک **Multi-Port** و ابزار قدرتمند **HAProxy**، اتصالات را بین چندین پورت توزیع می‌کند. این کار باعث می‌شود محدودیت‌های تک‌اتصالی شبکه رفع شده و پهنای باند به حداکثر پتانسیل خود برسد.

### 🛠 نحوه عملکرد (How it works)

* **در سرور ایران (IRAN Server):**
* اتصال را از کاربر روی یک پورت اصلی دریافت می‌کند.
* ترافیک را بین چندین پورت میانی تقسیم می‌کند.
* بسته‌ها را به پورت‌های متناظر در سرور خارج می‌فرستد.
* **مسیر:** **Client** → **IRAN:MainPort** → **IRAN:PortRange** → **KHAREJ:SamePorts** → **Destination**


* **در سرور خارج (KHAREJ Server):**
* روی چندین پورت گوش می‌دهد (Listen).
* دیتا را از پورت‌های مختلف سرور ایران تجمیع می‌کند.
* ترافیک نهایی را به پورت مقصد (مانند پورت پنل یا کانفیگ) هدایت می‌کند.



---

### ✨ قابلیت‌های کلیدی (Features)

* **نصب خودکار:** نصب و کانفیگ تمام پیش‌نیازها از جمله **HAProxy**.
* **توزیع بار (Load Balancing):** تقسیم هوشمند ترافیک برای خنثی کردن اثر **Packet Loss**.
* **سرعت حداکثری:** استفاده بهینه از پهنای باند با استفاده از چندین کانال ارتباطی.
* **پایداری بالا:** کاهش چشمگیر قطعی‌ها در زمان اختلالات شدید شبکه.
* **مدیریت آسان:** دارای منوی ساده برای نصب، تنظیم و حذف کامل سرویس.

---

### 📋 سیستم‌عامل‌های پشتیبانی شده

این اسکریپت روی توزیع‌های لینوکسی زیر تست شده و سازگار است:

* **Ubuntu** (پیشنهادی)
* **Debian**
* **CentOS**
* **AlmaLinux**
* **Rocky Linux**

---

### ⚙️ گزینه‌های منو (Menu Options)

پس از اجرای اسکریپت، با منوی زیر روبرو می‌شوید:

1. **IRAN SERVER:** جهت پیکربندی سرور مبدأ (ایران).
2. **KHAREJ SERVER:** جهت پیکربندی سرور مقصد (خارج).
3. **REMOVE / UNINSTALL:** توقف سرویس و پاک‌سازی کامل تنظیمات و **HAProxy**.

---

### 💎 مزایا

* عبور بهتر از محدودیت‌های **ISP**ها.
* کاهش تاخیر و بهبود تجربه کاربری در وب‌گردی و گیمینگ.

---

**نکته:** برای عملکرد صحیح، حتماً پورت‌های مورد نظر را در فایروال سرور خود باز کنید.

آیا مایل هستید بخش "پیش‌نیازها" یا "نمودار متنی" دقیق‌تری به این ردمی اضافه کنم؟
